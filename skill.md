---
name: "project-knowledge"
description: "自动分析任意工程项目并生成完整的 .claude/knowledge/ 知识库目录结构（INDEX.md + points.md + term-mapping.md + kbase/*.md）。当用户提到「建立知识库」、「初始化 knowledge」、「分析项目架构」、「生成知识目录」、「整理开发规范」时触发。支持指定目标路径或分析当前工作目录。"
---

# Project Analyzer

> 自动分析项目代码库并生成 Claude Code 知识库目录架构，解决通用大模型无法覆盖的专有知识召回问题。

---

## 核心使命

将任意项目代码库转化为 Claude Code 可理解的 `.claude/knowledge/` 目录结构，使 AI 在后续开发中能精确召回项目专有知识。

## 设计理念

基于多个工程项目的成功实践，提取出的可复用知识库构建规范：

1. **语义导航 + 精确检索** 双路召回机制
2. **领域语言统一** 通过术语映射表
3. **细粒度知识单元** 用于概念澄清和认知校正
4. **按领域深度组织** kbase 文档
5. **关键词触发机制** 精准召回相关知识

---

## 分析流程总览

```
[0. 安全检查]        验证路径 + 过滤敏感文件
     ↓
[1. 项目分类]        判定项目类型 → 确定维度优先级
     ↓
[2. 规模评估]        文件计数 → 确定分析深度层级
     ↓
[3. 缓存检查]        检查 auto memory 是否有上次分析
     ↓
[4. 分层扫描]        L1 配置 → L2 结构 → L3 采样 → L4 实体
     ↓  (交互确认)
[5. 知识目录生成]    按模板输出 7 个文件到 .claude/knowledge/
     ↓
[6. 写入缓存]        分析结果写入 auto memory 供下次复用
```

---

## 0. 安全检查

分析**任何**路径前必须先执行：

```
□ 目标路径是否存在？是否是目录？
□ 路径是否在用户明确指定的项目范围内？
□ 是否跳过了以下敏感文件类型？
   - .env, .env.*, credentials*, *.pem, *.key, *.cert
   - *.exe, *.dll, *.so, *.dylib, *.bin, *.class, *.wasm
   - *.jpg, *.jpeg, *.png, *.gif, *.ico, *.svg, *.woff, *.woff2, *.eot, *.ttf
   - *.mp3, *.mp4, *.avi, *.mov, *.zip, *.tar, *.gz, *.rar
   - node_modules/, .git/, .next/, dist/, build/, .cache/, vendor/
   - 任何经过 realpath 解析后不在项目目录内的文件
   - 任何大小超过 100KB 的文件（跳过内容，仅记录存在）
```

**安全响应格式**（如果检测到风险）：
```
⚠️ 路径安全检查未通过：
-  {具体问题}
请指定一个工程项目的根目录路径。
```

---

## 1. 项目分类器

分析正式开始前先判定项目类型，不同类型影响后续维度的权重和深度。

### 判定依据（按优先级）

| 信号文件 | 判定规则 |
|---------|---------|
| `package.json` | 有 `next` → Next.js 全栈；有 `react` → React SPA；仅 CLI 工具 → Node CLI |
| `pom.xml` / `build.gradle` | Java / Kotlin 项目 |
| `go.mod` | Go 项目 |
| `Cargo.toml` | Rust 项目 |
| `Gemfile` | Ruby / Rails 项目 |
| `requirements.txt` / `pyproject.toml` | Python 项目 |
| `Podfile` / `*.xcworkspace` | iOS / macOS 项目 |
| `pubspec.yaml` | Flutter / Dart 项目 |
| 多个语言的项目 | 按 `src/` 或 `lib/` 内容判定主要语言 |
| 以上都没有 | 通用项目（通用维度的子集） |

### 维度权重矩阵

| 项目类型 | 目录结构 | 技术栈 | 构建配置 | 代码架构 | API 设计 | 数据库 | 命名约定 | 业务领域 | 模式 |
|---------|:-------:|:-----:|:-------:|:-------:|:-------:|:-----:|:-------:|:-------:|:---:|
| Web 全栈 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ | ⭐⭐ |
| SPA 前端 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ | ⭐ | — | ⭐⭐⭐ | ⭐ | ⭐⭐⭐ |
| 后端 API | ⭐⭐ | ⭐⭐ | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ | ⭐⭐ | ⭐⭐ |
| CLI 工具 | ⭐⭐ | ⭐⭐ | ⭐⭐ | ⭐⭐ | — | — | ⭐⭐ | ⭐ | ⭐ |
| 数据/ML | ⭐⭐ | ⭐⭐⭐ | ⭐⭐ | ⭐⭐ | ⭐ | ⭐⭐ | ⭐ | ⭐⭐ | ⭐ |
| 移动端 | ⭐⭐⭐ | ⭐⭐ | ⭐⭐ | ⭐⭐⭐ | ⭐ | ⭐ | ⭐⭐ | ⭐⭐ | ⭐⭐ |
| Monorepo | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ | ⭐⭐ | ⭐⭐ | ⭐⭐ | ⭐⭐ |
| 通用 | ⭐⭐ | ⭐⭐ | ⭐ | ⭐ | ⭐ | ⭐ | ⭐⭐ | ⭐ | ⭐ |

（— 表示不分析该维度）

---

## 2. 规模评估与读取预算

根据项目文件总量动态决定分析深度。

### 规模分级

```
文件总数 = find . -type f \( -not -path '*/node_modules/*' -not -path '*/.git/*' -not -path '*/dist/*' -not -path '*/.next/*' \) | wc -l
```

| 规模 | 文件数 | 读取策略 | 分析深度 | 置信度告知 |
|------|-------|---------|---------|-----------|
| 小型 | < 50 | 全量读取所有关键文件 | 高 | ✅ "读取了大部分代码" |
| 中型 | 50 - 200 | 关键配置 + 每模块 2-3 个代表文件 | 中-高 | ✅ "基于代表性文件分析" |
| 大型 | 200 - 1000 | 关键配置 + 目录扫描 + 按模块抽样 10% | 中 | ⚠️ "基于抽样分析，可能遗漏" |
| 超大型 | > 1000 | 关键配置 + 目录 2 层 + 仅按需深入 | 低-中 | ⚠️ "仅概览，深入需追问" |

### 关键文件自动识别

按项目类型自动确定哪些文件是「关键文件」：

```
通用关键文件:
  - README*, LICENSE, CHANGELOG*, CONTRIBUTING*
  - package.json, composer.json, Cargo.toml, go.mod, Gemfile
  - tsconfig*, .gitlab-ci*, Dockerfile, docker-compose*
  - Makefile, justfile, Taskfile*

Web 项目附加:
  - next.config.*, nuxt.config.*, vite.config.*, webpack.config.*
  - tailwind.config.*, postcss.config.*
  - src/app/layout.*, src/pages/_app.*
  - middleware.*, auth.config.*

后端项目附加:
  - 主入口文件: main.go, main.py, index.js, app.js
  - 路由文件: routes/, controllers/, handlers/
  - ORM/配置文件: prisma/schema.prisma, ormconfig.*
  - 迁移目录: migrations/, db/migrate/
```

### 文件读取上限

```
关键配置:     完整读取
源文件:       每个最多 200 行
初始扫描:     总计最多 30 次文件读取
```

---

## 3. 缓存检查

如果该项目已被分析过，避免重复工作。

```
检查 ~/.claude/projects/<project-hash>/knowledge/last-scan.txt

[缓存命中] 如果存在:
  ├── < 7 天 → 直接使用缓存，告知用户「上次分析时间」
  └── > 7 天 → 增量扫描: find . -newer last-scan.txt 检测变更文件
                   ├── 有变更 → 部分重新扫描 + 更新缓存
                   └── 无变更 → 直接使用缓存

[缓存未命中] 继续全量扫描
```

`<project-hash>` 由项目绝对路径的 hash 生成。

---

## 4. 分层扫描（4 层渐进）

### L1：配置读取（固定预算 ≈ 2K tokens）

读取项目的关键配置文件，提取：

```
项目名称       → README.md 第一行 / package.json name
项目描述       → README.md / package.json description
主要语言       → 按文件扩展名统计
框架           → 从依赖推断
包管理器       → yarn.lock / package-lock.json / pnpm-lock.yaml / go.sum
构建脚本       → package.json scripts / Makefile targets
CI/CD          → .gitlab-ci.yml / .github/workflows/
Docker         → Dockerfile / docker-compose.yml
代码规范       → .eslintrc* / .prettierrc* / .golangci.yml
编辑器配置     → .editorconfig / .vscode/
```

### L2：结构扫描（固定预算 ≈ 1K tokens）

读取目录树，识别模块组织方式：

```
# 扫描深度按规模自适应
小型:   输出完整目录树
中型:   输出 src/ 和顶层结构
大型:   输出 src/ 顶层 + 各模块目录名
超大型: 仅输出顶层目录 + 关键子目录名
```

每个目录标注职责：
```
src/              ← 应用源码
├── components/   ← UI 组件
├── hooks/        ← 自定义 Hooks
├── lib/          ← 核心库
├── app/          ← 页面路由
└── api/          ← API 端点
```

### L3：模式采样（按需预算）

按模块读取代表性文件，提取模式和约定：

- **文件结构模式**（function 在前还是类型在前）
- **命名风格**（camelCase / snake_case / PascalCase / kebab-case）
- **错误处理模式**（try-catch / Result 类型 / error return）
- **导入组织**（绝对路径 vs 相对路径、分组方式）
- **注释密度**（是否有关键文档注释）

**采样规则**：
- 每个主要目录选 1-2 个非配置文件
- 优先选索引文件（index.ts, mod.go, __init__.py）
- 优先选路由文件、控制器文件

### L4：实体提取（按需预算）

从项目中提取业务领域的概念和术语：

| 来源 | 提取内容 |
|------|---------|
| 文件名 | 核心实体列表（从 `src/types/`, `src/models/`, `models/` 等目录的文件名提取） |
| 路由路径 | API 暴露的资源（`/api/tools`, `/api/users`） |
| 数据库 Schema | 表名、字段名（从 prisma schema / migration SQL / ORM 模型提取） |
| 类型定义 | 核心类型、接口（从 `types/`, `*.d.ts` 提取） |
| 中文注释/文档 | 业务术语的中文表达（从 README、注释中提取） |
| **完成度信号** | **package.json scripts（dev/build/typecheck/test）、CI 状态、README 的阶段描述、已知问题、测试文件存在性** |

---

## 5. 知识目录生成（核心输出）

分层扫描完成后，根据分析结果生成 `.claude/knowledge/` 目录下的 7 个文件。

### 5.0 生成前确认

生成文件前，先向用户展示分析摘要，获得确认后再写入：

```markdown
## 项目分析完成：{项目名称}

| 项目 | {名称} |
|------|--------|
| 类型 | {类型} |
| 语言 | {语言} |
| 框架 | {框架} |
| 数据库 | {数据库} |
| 代码量 | ~{N} 源文件 |

### 核心实体

{实体列表}

### 命名约定
- 文件命名：{kebab-case / PascalCase}
- 变量命名：{camelCase / snake_case}
- 数据库字段：{snake_case}
- API 字段：{camelCase}

### 关键发现
- {发现1}
- {发现2}
- {发现3}

### 实现程度速览
| 维度 | 状态 |
|------|------|
| 核心管线 | {Demo 完成 / Live 打通 / 等} |
| 类型检查 | {通过 / 未配置} |
| 测试 | {有 / 无} |
| 已知限制 | {主要限制概括} |

> 置信度：{高/中/低}（基于{全量/抽样/概览}分析）
> 分析时间：{N} 次文件读取

准备生成以下文件到 `.claude/knowledge/`：
- INDEX.md — 知识库索引
- points.md — 知识点库（预计 {N} 条）
- term-mapping.md — 术语映射表
- kbase/architecture.md — 架构知识
- kbase/api-design.md — API 设计知识
- kbase/database.md — 数据库知识
- kbase/frontend.md — 前端知识（如适用）

是否生成？(y/n)
```

用户确认后，执行第 5.1-5.7 节的文件生成。

### 5.1 生成 INDEX.md

知识库的主索引文件，定义双路召回机制和目录结构。

```markdown
# {项目名称} 知识库索引

> 知识库是组织化的知识存储体系，用于解决通用大模型无法覆盖的专有知识召回问题。

---

## 知识库结构

```
knowledge/
├── INDEX.md           # 本文件 — 知识库索引
├── points.md          # 知识点库（细粒度知识单元）
├── term-mapping.md    # 术语映射表
└── kbase/             # 知识库文档
    ├── architecture.md    # 架构知识
    ├── api-design.md      # API 设计知识
    ├── database.md        # 数据库知识
    └── frontend.md        # 前端知识
```

## 双路召回机制

### 1. KBase 语义召回
- **方式**：向量检索找「语义相关」的内容
- **适用**：模糊需求场景
- **特点**：覆盖面广，可能包含噪音

### 2. 索引导航式召回
- **方式**：先筛选文档 → 再精确搜索
- **适用**：精确定位场景
- **特点**：可追溯、可解释

## 知识库目录

### 架构知识 (architecture.md)
| 主题 | 描述 |
|------|------|
| 项目定位 | 什么项目、为谁做、解决什么问题 |
| 实现程度 | 各模块完成状态、已知限制 |
| 项目结构 | {目录组织规范} |
| 技术栈 | {技术栈描述} |
| 认证方案 | {认证方案描述} |
| 实体关系 | {核心实体列表} |

### API 设计知识 (api-design.md)
| 主题 | 描述 |
|------|------|
| RESTful 规范 | {API 规范描述} |
| 字段转换 | {命名转换规则} |
| 错误处理 | {错误响应格式} |
| 认证中间件 | {API 认证机制} |

### 数据库知识 (database.md)
| 主题 | 描述 |
|------|------|
| 表结构设计 | {表设计规范} |
| 迁移流程 | {迁移规范} |
| 索引策略 | {索引设计规范} |
| 查询优化 | {常用查询模式} |

### 前端知识 (frontend.md)（如适用）
| 主题 | 描述 |
|------|------|
| 组件规范 | {组件设计规范} |
| 样式规范 | {样式使用规范} |
| 状态管理 | {状态管理方案} |

## 知识召回触发

| 关键词 | 召回知识库 |
|--------|-----------|
| API、接口、后端 | api-design.md |
| 数据库、表、SQL | database.md |
| 组件、页面、样式 | frontend.md |
| 架构、目录、结构 | architecture.md |
| 项目定位、项目目标、这是什么、为谁做 | architecture.md（第零章） |
| 实现程度、完成度、能用吗、已知限制 | architecture.md（第五章） |

---

*由 Project Knowledge 于 {YYYY-MM-DD} 自动生成*
```

### 5.2 生成 points.md

细粒度知识单元库。从 L3 模式采样和 L4 实体提取中提取知识点。

**知识点分类**：

| 分类 | 编号 | 来源 |
|------|------|------|
| 项目定位 | KP-001 ~ KP-003 | L1 配置 + README：项目目标、目标用户、核心交互 |
| 代码定位 | KP-004 ~ KP-012 | L2 结构扫描：核心文件位置 |
| 业务术语 | KP-013 ~ KP-022 | L4 实体提取：领域概念 |
| 技术决策 | KP-023 ~ KP-032 | L1 配置读取：技术选型理由 |
| 实现程度 | KP-033 ~ KP-036 | L3 模式采样 + 测试/构建脚本：各模块完成状态、已知限制 |
| 常见陷阱 | KP-037 ~ KP-046 | L3 模式采样：已知坑和注意事项 |
| 项目规范 | KP-047 ~ KP-056 | L3 模式采样：Git 规范、编码规范 |

**知识点模板**：

```markdown
### KP-NNN: {知识点标题}
| 字段 | 内容 |
|------|------|
| **触发关键词** | keyword1, keyword2, ... |
| **知识点** | {核心知识描述} |
| **相关文件** | {文件路径} |
```

**知识点索引表**：每添加一个知识点，在文件末尾的索引表中追加：

```markdown
| KP-NNN | {类型} | {关键词} |
```

**知识点质量规则**：
- 唯一性：每个知识点覆盖一个独立概念
- 可触发：触发关键词必须是实际会被需求描述使用的自然语言
- 精准定位：必须包含准确的代码路径，而非模糊描述
- 可操作：读者看到后应能直接采取行动

### 5.3 生成 term-mapping.md

术语映射表，解决业务 ≠ 代码 ≠ API 的命名脱节问题。

**实体术语映射**（从 L4 实体提取生成）：

```
| 业务术语 | 英文术语 | 代码命名 | API 路径 | 数据库表 |
|---------|---------|---------|---------|---------|
| {术语} | {Term} | `{Pascal}` / `{camel}` | `/api/{path}` | `{table}` |
```

**字段术语映射**（从 L3 命名采样生成）：

```
| 业务术语 | 英文术语 | 数据库字段 | API 字段 | TypeScript |
|---------|---------|-----------|---------|------------|
| {术语} | {Name} | `{snake}` | `{camel}` | `{camel}: {type}` |
```

**状态术语映射**（如果发现枚举状态）：

```
| 业务术语 | 英文术语 | 数据库值 | 说明 |
|---------|---------|---------|------|
| {术语} | {Term} | {value} | {说明} |
```

**易混淆术语**（如果发现常见错误命名）：

```
| ❌ 错误用法 | ✅ 正确用法 | 说明 |
|------------|------------|------|
| {wrong} | {correct} | {说明} |
```

**新术语添加规范**：

```
当引入新业务概念时，按以下步骤添加：
1. 确定业务术语：与用户确认中文名称
2. 确定英文术语：使用单数形式
3. 确定代码命名：遵循 camelCase/PascalCase
4. 确定 API 路径：使用 kebab-case 复数形式
5. 更新本文件：添加到对应映射表
```

### 5.4 生成 kbase/architecture.md

项目架构知识文档，从 L1 配置提取 + L2 结构扫描生成。

**架构模板分为 6 章**：项目定位 → 项目结构 → 技术栈 → 认证方案 → 实体关系 → 实现程度。前两章回答「这是什么项目」「当前能用吗」，是 AI 协作中最高频需要的信息。

```markdown
# 架构知识

> 本文档沉淀 {项目名称} 项目的架构相关知识。

---

## 零、项目定位

### 这是什么？

{一句话定义：什么类型的项目，解决什么问题}

### 目标用户

{用户画像}

### 核心交互模式

| 区域 | 内容 | 说明 |
|------|------|------|
| ... | ... | ... |

### 为什么有这个项目

{项目背景和动机描述}

---

## 一、项目结构

```
{从 L2 扫描生成的目录树}
```

## 二、技术栈

### 前端
| 技术 | 版本 | 用途 |
|------|------|------|
| {技术} | {版本} | {用途} |

### 后端
| 技术 | 版本 | 用途 |
|------|------|------|
| {技术} | {版本} | {用途} |

### 工具链
| 工具 | 用途 |
|------|------|
| {工具} | {用途} |

## 三、认证方案（如适用）

{认证方案描述}

## 四、实体关系

| 实体 | 说明 | 关键字段 |
|------|------|---------|
| {实体} | {说明} | {字段} |

## 五、实现程度

### 总览

| 维度 | 状态 | 说明 |
|------|------|------|
| {核心功能} | {✅ 完成 / 🟡 部分 / ⬜ 未开始} | {说明} |
| 类型检查 | {状态} | |
| 测试 | {状态} | |
| 构建 | {状态} | |

### 各模块详情

| 模块 | 完成度 | 说明 |
|------|--------|------|
| {模块1} | {完成/部分/未开始} | {当前行为描述} |
| {模块2} | ... | ... |

### 已知限制

1. {限制1}
2. {限制2}

---

*由 Project Knowledge 于 {YYYY-MM-DD} 自动生成*
```

### 5.5 生成 kbase/api-design.md

API 设计知识文档，从 L2 路由扫描 + L3 采样生成。

```markdown
# API 设计知识

> 本文档沉淀 {项目名称} 项目的 API 设计相关知识。

---

## 一、API 路由规范

### 路由结构

```
{API 路由树}
```

### HTTP 方法映射

| 方法 | 用途 |
|------|------|
| GET | 获取资源 |
| POST | 创建资源 |
| PUT | 更新资源 |
| DELETE | 删除资源 |

## 二、请求响应规范

### 字段转换规则
- 数据库字段：{snake_case / camelCase}
- API 返回字段：{snake_case / camelCase}
- 转换位置：{工具函数路径}

### 统一响应格式

```typescript
// 成功响应
{
  success: true,
  data: T,
  message?: string
}

// 错误响应
{
  success: false,
  error: string,
  code?: string
}
```

## 三、错误处理

| 状态码 | 含义 | 使用场景 |
|--------|------|---------|
| 200 | 成功 | 请求成功 |
| 400 | 请求错误 | 参数验证失败 |
| 401 | 未认证 | 未登录或 token 过期 |
| 404 | 未找到 | 资源不存在 |
| 500 | 服务器错误 | 内部错误 |

---

*由 Project Knowledge 于 {YYYY-MM-DD} 自动生成*
```

### 5.6 生成 kbase/database.md

数据库知识文档，从 L4 实体提取中的数据库信息生成。

```markdown
# 数据库知识

> 本文档沉淀 {项目名称} 项目的数据库相关知识。

---

## 一、数据库配置

| 配置项 | 值 |
|--------|-----|
| 数据库 | {数据库类型和版本} |
| 连接方式 | {连接方式} |
| 连接池 | {是否启用} |

## 二、表结构规范

### 命名规范
| 规则 | 示例 |
|------|------|
| 表名 | {命名规则} |
| 字段 | {命名规则} |
| 主键 | {命名规则} |
| 外键 | {命名规则} |

## 三、主要表结构

{从 L4 提取的主要表 DDL，选取最重要的 3-5 个表}

## 四、迁移规范

{迁移文件位置和命名规则}

## 五、索引策略

{关键索引列表}

---

*由 Project Knowledge 于 {YYYY-MM-DD} 自动生成*
```

### 5.7 生成 kbase/frontend.md（如适用）

仅当项目类型属于 Web 全栈或 SPA 前端时生成。

```markdown
# 前端知识

> 本文档沉淀 {项目名称} 项目的前端相关知识。

---

## 一、组件规范

{从 L3 采样提取的组件结构和命名规范}

## 二、样式规范

{从 L3 采样提取的样式方案和约定}

## 三、状态管理（如适用）

{从 L3 采样提取的状态管理方案}

---

*由 Project Knowledge 于 {YYYY-MM-DD} 自动生成*
```

---

## 6. 写入缓存

分析结果写入 `~/.claude/projects/<project-hash>/knowledge/` 供跨会话复用：

```
~/.claude/projects/<project-hash>/
└── knowledge/
    ├── project-summary.md   # L1 配置摘要 + 项目类型
    ├── structure.md         # L2 目录结构
    ├── entity-map.md        # L4 实体提取
    └── last-scan.txt        # 扫描时间戳
```

---

## 输出结构总览

```
生成到目标项目的 .claude/knowledge/ 目录：

.claude/knowledge/
├── INDEX.md                # 知识库索引（主入口）
├── points.md               # 知识点库（细粒度知识单元）
├── term-mapping.md         # 术语映射表
└── kbase/                  # 知识库文档
    ├── architecture.md     # 架构知识
    ├── api-design.md       # API 设计知识
    ├── database.md         # 数据库知识
    └── frontend.md         # 前端知识（如适用）
```

---

## 使用规则

1. **项目根目录检测**
   - 自动检测项目根目录
   - 如果目标路径已有 `.claude/knowledge/`，执行增量更新

2. **增量更新支持**
   - 检测现有知识点编号，新知识点从最大编号 +1 开始
   - 跳过已存在的重复知识点（按关键词匹配去重）
   - 只更新有变化的部分，不重写整个知识库

3. **不覆盖用户修改**
   - 如果生成后发现目标文件已有用户自定义内容，只追加不覆盖
   - 在文件头部的 `*由 Project Knowledge 自动生成*` 标记改为 `*由 Project Knowledge 初始生成，后续有手动修改*`

4. **可配置化**
   - 支持自定义实体类型列表
   - 支持忽略特定目录或文件
   - 支持添加自定义知识分类

---

## 上下文预算管理

### 初始回复预算
初始分析结果展示**不超过 3000 tokens**。超出部分截断：
- L4 实体提取只保留前 10 个最有价值的实体
- 只列实体名称，不展开字段细节
- 告知用户「详细实体字段可在生成的文件中查看」

### 生成阶段的上下文策略
知识目录文件生成是**多次独立 Write 操作**，不将所有文件内容同时加载到上下文：
1. 逐个读取分析缓存
2. 逐个生成模板内容
3. 逐个写入文件
4. 每个文件生成后立即从上下文移除（保留摘要引用）

---

## 注意事项

### 务必做
- 每次分析结果前标注置信度（基于全量/抽样/概览）
- 生成文件前获得用户确认（展示分析摘要）
- 提及具体文件路径时使用相对于项目根目录的路径
- 确保所有生成文件的 `*最后更新*` 时间戳使用当日日期

### 务必不做
- 不读取敏感文件（.env、凭据、私钥）
- 不读取二进制文件和大文件（>100KB）
- 不覆盖用户已手动修改的知识库文件
- 不生成空文件（如果某个维度无数据，跳过该文件）
- 不输出未经确认的架构推断

---

## 质量门禁与 Definition of Done

每个文件生成后立即执行对应 DoD 检查，**不满足不推进**。

### INDEX.md DoD
- [ ] 双路召回机制描述完整
- [ ] 知识库目录与实际分析结果一致（不包含无数据的分类）
- [ ] 关键词触发表已填充，关键词为实际会被使用的自然语言
- [ ] 模板中的 `{占位符}` 已全部替换为实际内容

### points.md DoD
- [ ] 至少 3 个知识点
- [ ] 每个知识点包含触发关键词、核心知识、相关文件
- [ ] 知识点覆盖至少 2 个分类
- [ ] 知识点质量规则已遵循（唯一性/可触发/精准定位/可操作）

### term-mapping.md DoD
- [ ] 至少包含实体映射表
- [ ] 业务术语 ↔ 代码命名 ↔ API 路径 双向可追溯
- [ ] 如有枚举状态，已包含状态术语映射表

### kbase/ 各文件 DoD
- [ ] 占位符已全部替换为实际内容
- [ ] 内容基于实际分析结果，而非推断
- [ ] 仅当项目类型适用时生成（如 frontend.md 仅对 Web 项目）
- [ ] 每个文件有至少一个实质性的表格或内容块
- [ ] architecture.md 包含项目定位（第零章）和实现程度（最后一章）
- [ ] 实现程度章节包含：总览表、各模块详情、已知限制

### 全局 DoD（所有文件生成完成后）
- [ ] 所有文件的 `*最后更新*` 时间戳使用当日日期
- [ ] 生成的文件数与 5.0 确认时承诺的一致
- [ ] 无空文件（如某维度无数据，跳过该文件）
- [ ] 不覆盖用户已有手动修改的内容

---

*最后更新: 2026-05-15*
